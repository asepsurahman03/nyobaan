import React from 'react';
import Table from '@/Components/Table';

const Form = () => {
  // Contoh data
  // const posts = [];

  return (
    <div className="container mx-auto">
      <Table />
    </div>
  );
};

export default Form;
